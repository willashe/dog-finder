import React from 'react';
import { FaTree } from 'react-icons/fa';

const Park = ({ data }) => {
  const style = {
    fontSize: 48,
    color: '#315659',
  };

  return <FaTree style={style} />;
};

export default Park;